import defaultSettings from '@/settings'

const { tagShow, logoShow } = defaultSettings

const state = {
  tagShow: tagShow,
  logoShow: logoShow
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
